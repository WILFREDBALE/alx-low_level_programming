#include "main"
#include <stdio.h>

/**
 * mul - multiplies 2 numbers
 * @a: first number to be multiplied by b
 * @b: second number to be multiplied by a
 * Return: Returns the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
