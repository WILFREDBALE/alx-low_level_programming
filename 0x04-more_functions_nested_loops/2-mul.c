#include "main"
#include <stdio.h>

/**
 * mul - multiplies 2 numbers
 * @a: first number
 * @b: second number
 * Return: return the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
