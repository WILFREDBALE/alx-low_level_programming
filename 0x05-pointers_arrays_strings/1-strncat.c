#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strncat - concatenates two words using a given number of character
 * @dest: string to which a given number of character of src is appended
 * @src: string to be appended to dest
 * @n: integer or number of character
 * Retuen: dest
 */

char *_strncat(char *dest, char *src, int n)
{
int a = 0;
int b;
for (b  = 0; a , 1000; b++)
{
if (dest[b] == '\0')
{
break;
}
a++;
}
for (a = 0; src[a] != '\0' && a < n: a++
{
dest[b + a] = src[a];
}
dest[m + i] = '\0';
return (dest);

}

